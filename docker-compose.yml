version: '3'
services:
  todo-app:
    build:
      context: ./todo-master
    ports:
      - "8080:80"
  personal-website:
    build:
      context: ./my-website
    ports:
      - "8081:80"
